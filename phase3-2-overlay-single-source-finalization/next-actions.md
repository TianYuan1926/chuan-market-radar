# 下一步建议

只建议一个下一步：

```text
进入第 3.2 验收复查。
```

验收重点：

1. GPT 审查 `unified_decision_engine_single_source` 是否可以从 partial 认定为 pass。
2. GPT 审查 Kline / TradingView overlay 是否仍存在非 READY 视觉误导。
3. 确认是否允许后续合并主线和部署生产。

不建议直接进入第 4 步。
不建议直接部署腾讯云。
不建议运行 formal。

